---
// Header of a dated entry — an experience or a degree: number (experiences only), title, date. Style: EntryHeader.css.
import { md } from '../lib/text';
interface Props { title: string; date: string; num?: string }   // title: already formatted HTML (bold, dash)
const { title, date, num } = Astro.props;
---
<div class="job-header"><div class="job-title-line">
  {num && <span class="job-num label">{num}</span>}
  <h3 class="job-title" set:html={title} />
  <span class="job-date" set:html={md(date)} />
</div></div>
