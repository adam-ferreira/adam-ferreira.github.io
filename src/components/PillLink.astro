---
// A link drawn as a pill: the contact details of the hero (with an icon), the way back of the 404 page. Style: PillLink.css.
interface Props { href: string }
const { href } = Astro.props;
---
<a class="pill" href={href}><slot /></a>
