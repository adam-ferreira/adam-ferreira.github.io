// The visitor's light / dark choice, remembered under this key: read before the first paint by the inline boot script
// (src/components/Head.astro), then by the switch (src/components/ThemeToggle.js).
export const THEME_KEY = 'cv-theme';
